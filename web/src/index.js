import React from 'react';
import ReactDOM from 'react-dom'; // arvore de elementos do html
import App from './App';

ReactDOM.render(<App />, document.getElementById('root'));
// JSX (JS + XML (ou HTML))